import { render, screen } from "@testing-library/react";
import WelcomeText from "./WelcomeText";

test("renders the landing page text", () => {
  // Need to fix failing tests due to React Router
});
